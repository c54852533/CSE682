<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.su.cse682.infrastructure.persistent.user.dao.base.UserAuthDoBaseMapper">


    <insert id="insertUserAuthDo" >
        INSERT INTO `user_auth`
        (
        <trim suffixOverrides=",">
                    <if test="uuid!=null">
                        `uuid`,
                    </if>
                    <if test="gmtCreate!=null">
                        `gmt_create`,
                    </if>
                    <if test="gmtModified!=null">
                        `gmt_modified`,
                    </if>
                    <if test="userId!=null">
                        `user_id`,
                    </if>
                    <if test="identityType!=null">
                        `identity_type`,
                    </if>
                    <if test="identifier!=null">
                        `identifier`,
                    </if>
                    <if test="credential!=null">
                        `credential`,
                    </if>
        </trim>
        )
        VALUES
        (
        <trim suffixOverrides=",">
                <if test="uuid!=null">
                    #{uuid},
                </if>
                <if test="gmtCreate!=null">
                    #{gmtCreate},
                </if>
                <if test="gmtModified!=null">
                    #{gmtModified},
                </if>
                <if test="userId!=null">
                    #{userId},
                </if>
                <if test="identityType!=null">
                    #{identityType},
                </if>
                <if test="identifier!=null">
                    #{identifier},
                </if>
                <if test="credential!=null">
                    #{credential},
                </if>
        </trim>
        )
    </insert>




    <update id="updateUserAuthDo">
        UPDATE `user_auth`
        SET
        <trim suffixOverrides=",">
            <if test="gmtCreate != null">
                `gmt_create` = #{gmtCreate},
            </if>
            <if test="gmtModified != null">
                `gmt_modified` = #{gmtModified},
            </if>
            <if test="userId != null and userId!=''">
                `user_id` = #{userId},
            </if>
            <if test="identityType != null and identityType!=''">
                `identity_type` = #{identityType},
            </if>
            <if test="identifier != null and identifier!=''">
                `identifier` = #{identifier},
            </if>
            <if test="credential != null and credential!=''">
                `credential` = #{credential},
            </if>
        </trim>
        WHERE
        <trim suffixOverrides="and">
                `uuid` = #{uuid} and
        </trim>
    </update>

    <update id="update">
        UPDATE `user_auth`
        SET
        <trim suffixOverrides=",">
        <if test="set.gmtCreate != null">
            `gmt_create` = #{set.gmtCreate},
        </if>
        <if test="set.gmtModified != null">
            `gmt_modified` = #{set.gmtModified},
        </if>
        <if test="set.userId != null and set.userId!=''">
            `user_id` = #{set.userId},
        </if>
        <if test="set.identityType != null and set.identityType!=''">
            `identity_type` = #{set.identityType},
        </if>
        <if test="set.identifier != null and set.identifier!=''">
            `identifier` = #{set.identifier},
        </if>
        <if test="set.credential != null and set.credential!=''">
            `credential` = #{set.credential},
        </if>
        </trim>
        <trim prefix="where" suffixOverrides="and | or">
                <if test="where.uuidList != null">
                    `uuid` in
                    <foreach collection="where.uuidList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyUuid!=null and where.fuzzyUuid.size()>0">
                (
                <foreach collection="where.fuzzyUuid"  separator="or" item="item">
                    `Uuid` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyUuid!=null and where.rightFuzzyUuid.size()>0">
                (
                <foreach collection="where.rightFuzzyUuid"  separator="or" item="item">
                    `Uuid` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.gmtCreateList != null">
                    `gmt_create` in
                    <foreach collection="where.gmtCreateList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test="where.gmtCreateSt !=null">
                `gmt_create` >= #{where.gmtCreateSt} and
            </if>
            <if test="where.gmtCreateEd!=null">
                `gmt_create` &lt;= #{where.gmtCreateEd} and
            </if>
                <if test="where.gmtModifiedList != null">
                    `gmt_modified` in
                    <foreach collection="where.gmtModifiedList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test="where.gmtModifiedSt !=null">
                `gmt_modified` >= #{where.gmtModifiedSt} and
            </if>
            <if test="where.gmtModifiedEd!=null">
                `gmt_modified` &lt;= #{where.gmtModifiedEd} and
            </if>
                <if test="where.userIdList != null">
                    `user_id` in
                    <foreach collection="where.userIdList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyUserId!=null and where.fuzzyUserId.size()>0">
                (
                <foreach collection="where.fuzzyUserId"  separator="or" item="item">
                    `User_id` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyUserId!=null and where.rightFuzzyUserId.size()>0">
                (
                <foreach collection="where.rightFuzzyUserId"  separator="or" item="item">
                    `User_id` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.identityTypeList != null">
                    `identity_type` in
                    <foreach collection="where.identityTypeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyIdentityType!=null and where.fuzzyIdentityType.size()>0">
                (
                <foreach collection="where.fuzzyIdentityType"  separator="or" item="item">
                    `Identity_type` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyIdentityType!=null and where.rightFuzzyIdentityType.size()>0">
                (
                <foreach collection="where.rightFuzzyIdentityType"  separator="or" item="item">
                    `Identity_type` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.identifierList != null">
                    `identifier` in
                    <foreach collection="where.identifierList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyIdentifier!=null and where.fuzzyIdentifier.size()>0">
                (
                <foreach collection="where.fuzzyIdentifier"  separator="or" item="item">
                    `Identifier` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyIdentifier!=null and where.rightFuzzyIdentifier.size()>0">
                (
                <foreach collection="where.rightFuzzyIdentifier"  separator="or" item="item">
                    `Identifier` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.credentialList != null">
                    `credential` in
                    <foreach collection="where.credentialList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyCredential!=null and where.fuzzyCredential.size()>0">
                (
                <foreach collection="where.fuzzyCredential"  separator="or" item="item">
                    `Credential` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyCredential!=null and where.rightFuzzyCredential.size()>0">
                (
                <foreach collection="where.rightFuzzyCredential"  separator="or" item="item">
                    `Credential` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
        </trim>
    </update>


    <resultMap type="com.su.cse682.infrastructure.persistent.user.UserAuthDo" id="UserAuthDoMap">
        <result property="uuid" column="uuid"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
        <result property="userId" column="user_id"/>
        <result property="identityType" column="identity_type"/>
        <result property="identifier" column="identifier"/>
        <result property="credential" column="credential"/>
    </resultMap>


    <select id="queryUserAuthDo" resultMap="UserAuthDoMap">
        select
        <include refid="baseResult"></include>
        from  `user_auth`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="uuid != null and uuid!=''">
                `uuid` = #{uuid} and
            </if>
            <if test="gmtCreate != null">
                `gmt_create` = #{gmtCreate} and
            </if>
            <if test="gmtModified != null">
                `gmt_modified` = #{gmtModified} and
            </if>
            <if test="userId != null and userId!=''">
                `user_id` = #{userId} and
            </if>
            <if test="identityType != null and identityType!=''">
                `identity_type` = #{identityType} and
            </if>
            <if test="identifier != null and identifier!=''">
                `identifier` = #{identifier} and
            </if>
            <if test="credential != null and credential!=''">
                `credential` = #{credential} and
            </if>
            <if test = "(_parameter instanceof com.su.cse682.infrastructure.persistent.user.UserAuthDo$QueryBuilder) == true">
                <if test="uuidList != null">
                    `uuid` in
                    <foreach collection="uuidList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyUuid!=null and fuzzyUuid.size()>0">
                    (
                    <foreach collection="fuzzyUuid"  separator="or" item="item">
                        `Uuid` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyUuid!=null and rightFuzzyUuid.size()>0">
                    (
                    <foreach collection="rightFuzzyUuid"  separator="or" item="item">
                        `Uuid` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="gmtCreateList != null">
                    `gmt_create` in
                    <foreach collection="gmtCreateList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test="gmtCreateSt !=null">
                    `gmt_create` >= #{gmtCreateSt} and
                </if>
                <if test="gmtCreateEd!=null">
                    `gmt_create` &lt;= #{gmtCreateEd} and
                </if>
                <if test="gmtModifiedList != null">
                    `gmt_modified` in
                    <foreach collection="gmtModifiedList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test="gmtModifiedSt !=null">
                    `gmt_modified` >= #{gmtModifiedSt} and
                </if>
                <if test="gmtModifiedEd!=null">
                    `gmt_modified` &lt;= #{gmtModifiedEd} and
                </if>
                <if test="userIdList != null">
                    `user_id` in
                    <foreach collection="userIdList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyUserId!=null and fuzzyUserId.size()>0">
                    (
                    <foreach collection="fuzzyUserId"  separator="or" item="item">
                        `User_id` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyUserId!=null and rightFuzzyUserId.size()>0">
                    (
                    <foreach collection="rightFuzzyUserId"  separator="or" item="item">
                        `User_id` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="identityTypeList != null">
                    `identity_type` in
                    <foreach collection="identityTypeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyIdentityType!=null and fuzzyIdentityType.size()>0">
                    (
                    <foreach collection="fuzzyIdentityType"  separator="or" item="item">
                        `Identity_type` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyIdentityType!=null and rightFuzzyIdentityType.size()>0">
                    (
                    <foreach collection="rightFuzzyIdentityType"  separator="or" item="item">
                        `Identity_type` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="identifierList != null">
                    `identifier` in
                    <foreach collection="identifierList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyIdentifier!=null and fuzzyIdentifier.size()>0">
                    (
                    <foreach collection="fuzzyIdentifier"  separator="or" item="item">
                        `Identifier` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyIdentifier!=null and rightFuzzyIdentifier.size()>0">
                    (
                    <foreach collection="rightFuzzyIdentifier"  separator="or" item="item">
                        `Identifier` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="credentialList != null">
                    `credential` in
                    <foreach collection="credentialList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyCredential!=null and fuzzyCredential.size()>0">
                    (
                    <foreach collection="fuzzyCredential"  separator="or" item="item">
                        `Credential` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyCredential!=null and rightFuzzyCredential.size()>0">
                    (
                    <foreach collection="rightFuzzyCredential"  separator="or" item="item">
                        `Credential` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
            </if>
        </trim>
    </select>

    <select id="queryUserAuthDoLimit1" resultMap="UserAuthDoMap">
        select
        <include refid="baseResult"></include>
        from  `user_auth`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="uuid != null and uuid!=''">
                `uuid` = #{uuid} and
            </if>
            <if test="gmtCreate != null">
                `gmt_create` = #{gmtCreate} and
            </if>
            <if test="gmtModified != null">
                `gmt_modified` = #{gmtModified} and
            </if>
            <if test="userId != null and userId!=''">
                `user_id` = #{userId} and
            </if>
            <if test="identityType != null and identityType!=''">
                `identity_type` = #{identityType} and
            </if>
            <if test="identifier != null and identifier!=''">
                `identifier` = #{identifier} and
            </if>
            <if test="credential != null and credential!=''">
                `credential` = #{credential} and
            </if>
            <if test = "(_parameter instanceof com.su.cse682.infrastructure.persistent.user.UserAuthDo$QueryBuilder) == true">
                    <if test="uuidList != null">
                        `uuid` in
                        <foreach collection="uuidList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyUuid!=null and fuzzyUuid.size()>0">
                    (
                    <foreach collection="fuzzyUuid"  separator="or" item="item">
                        `Uuid` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyUuid!=null and rightFuzzyUuid.size()>0">
                    (
                    <foreach collection="rightFuzzyUuid"  separator="or" item="item">
                        `Uuid` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="gmtCreateList != null">
                        `gmt_create` in
                        <foreach collection="gmtCreateList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test="gmtCreateSt !=null">
                    `gmt_create` >= #{gmtCreateSt} and
                </if>
                <if test="gmtCreateEd!=null">
                    `gmt_create` &lt;= #{gmtCreateEd} and
                </if>
                    <if test="gmtModifiedList != null">
                        `gmt_modified` in
                        <foreach collection="gmtModifiedList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test="gmtModifiedSt !=null">
                    `gmt_modified` >= #{gmtModifiedSt} and
                </if>
                <if test="gmtModifiedEd!=null">
                    `gmt_modified` &lt;= #{gmtModifiedEd} and
                </if>
                    <if test="userIdList != null">
                        `user_id` in
                        <foreach collection="userIdList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyUserId!=null and fuzzyUserId.size()>0">
                    (
                    <foreach collection="fuzzyUserId"  separator="or" item="item">
                        `User_id` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyUserId!=null and rightFuzzyUserId.size()>0">
                    (
                    <foreach collection="rightFuzzyUserId"  separator="or" item="item">
                        `User_id` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="identityTypeList != null">
                        `identity_type` in
                        <foreach collection="identityTypeList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyIdentityType!=null and fuzzyIdentityType.size()>0">
                    (
                    <foreach collection="fuzzyIdentityType"  separator="or" item="item">
                        `Identity_type` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyIdentityType!=null and rightFuzzyIdentityType.size()>0">
                    (
                    <foreach collection="rightFuzzyIdentityType"  separator="or" item="item">
                        `Identity_type` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="identifierList != null">
                        `identifier` in
                        <foreach collection="identifierList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyIdentifier!=null and fuzzyIdentifier.size()>0">
                    (
                    <foreach collection="fuzzyIdentifier"  separator="or" item="item">
                        `Identifier` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyIdentifier!=null and rightFuzzyIdentifier.size()>0">
                    (
                    <foreach collection="rightFuzzyIdentifier"  separator="or" item="item">
                        `Identifier` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="credentialList != null">
                        `credential` in
                        <foreach collection="credentialList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyCredential!=null and fuzzyCredential.size()>0">
                    (
                    <foreach collection="fuzzyCredential"  separator="or" item="item">
                        `Credential` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyCredential!=null and rightFuzzyCredential.size()>0">
                    (
                    <foreach collection="rightFuzzyCredential"  separator="or" item="item">
                        `Credential` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
            </if>
        </trim>
        limit 1
    </select>

    <sql id="allResult">
        <trim suffixOverrides=",">
        `uuid`,
        `gmt_create`,
        `gmt_modified`,
        `user_id`,
        `identity_type`,
        `identifier`,
        `credential`,
        </trim>
    </sql>



    <sql id="baseResult">
        <trim suffixOverrides=",">
            <if test = "(_parameter instanceof com.su.cse682.infrastructure.persistent.user.UserAuthDo$QueryBuilder) == true">

                <if test="fetchFields==null">
                    <include refid="allResult"></include>
                </if>
                <if test="fetchFields!=null">
                    <if test="fetchFields.AllFields !=null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields ==null and fetchFields.fetchFields==null and fetchFields.excludeFields==null and fetchFields.otherFields==null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.fetchFields!=null">
                    <if test="fetchFields.fetchFields.uuid==true">
                        `uuid`,
                    </if>
                    <if test="fetchFields.fetchFields.gmtCreate==true">
                        `gmt_create`,
                    </if>
                    <if test="fetchFields.fetchFields.gmtModified==true">
                        `gmt_modified`,
                    </if>
                    <if test="fetchFields.fetchFields.userId==true">
                        `user_id`,
                    </if>
                    <if test="fetchFields.fetchFields.identityType==true">
                        `identity_type`,
                    </if>
                    <if test="fetchFields.fetchFields.identifier==true">
                        `identifier`,
                    </if>
                    <if test="fetchFields.fetchFields.credential==true">
                        `credential`,
                    </if>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.excludeFields!=null">
                    <if test="fetchFields.excludeFields.uuid==null">
                        `uuid`,
                    </if>
                    <if test="fetchFields.excludeFields.gmtCreate==null">
                        `gmt_create`,
                    </if>
                    <if test="fetchFields.excludeFields.gmtModified==null">
                        `gmt_modified`,
                    </if>
                    <if test="fetchFields.excludeFields.userId==null">
                        `user_id`,
                    </if>
                    <if test="fetchFields.excludeFields.identityType==null">
                        `identity_type`,
                    </if>
                    <if test="fetchFields.excludeFields.identifier==null">
                        `identifier`,
                    </if>
                    <if test="fetchFields.excludeFields.credential==null">
                        `credential`,
                    </if>
                    </if>
                    <if test="fetchFields.otherFields!=null and fetchFields.otherFields.size>0">
                        <foreach collection="fetchFields.otherFields" index="index" item="item" separator=",">
                        `#{item}`
                        </foreach>
                    </if>
                </if>
            </if>
            <if test="(_parameter instanceof com.su.cse682.infrastructure.persistent.user.UserAuthDo$QueryBuilder) == false" >
                <include refid="allResult"></include>
            </if>

        </trim>
    </sql>


</mapper>